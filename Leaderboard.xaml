<Page x:Class="SeeMyPoints.Leaderboard"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:SeeMyPoints"
      mc:Ignorable="d"
      Title="Leaderboard" MinHeight="650" HorizontalAlignment="Stretch" MinWidth="900" VerticalAlignment="Stretch">
    <Grid>
        <!-- Contenu principal -->
        <Button Content="Ouvrir Popup" Click="OpenPopup_Click" Width="120" Height="40" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10"/>

        <!-- Popup -->
        <Popup Name="MyPopup" StaysOpen="False" Placement="Center" AllowsTransparency="True">
            <Border Background="#333333" Width="300" Height="200" CornerRadius="10" BorderBrush="White" BorderThickness="1">
                <Grid>
                    <TextBlock Text="Ceci est une popup!" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" FontSize="16"/>
                    <Button Content="Fermer" Width="80" Height="30" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,20" Click="ClosePopup_Click"/>
                </Grid>
            </Border>
        </Popup>
    </Grid>
</Page>
